# Ink DSL for web quests

## Setup:

    yarn install

## Run the story in the web browser:

    yarn start

Then navigate to http://localhost:8081/ .

## Run the story in the node console (CLI):

    node src/console.js
